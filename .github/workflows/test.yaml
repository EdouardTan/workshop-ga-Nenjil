name: learn-github-actions
on:
    push:
        branches:
            - dev
            - master
            - main
jobs:
    hello-world:
        runs-on: ubuntu-latest
        steps:
         - name: hello-world
          shell: bash
          run: echo "Hello World"